# FoxLearn Demo 环境说明

> 面向客户/评审的可演示环境。所有演示账号密码统一：**foxlearn2026**

## 1. 访问地址

| 项 | 值 |
|----|----|
| 首页 | http://<服务器IP>/（部署后由 `deploy/setup-demo.sh` 自动写入 `.env` 的 `SITE_URL`） |
| 后端 API | http://<服务器IP>/api（Nginx 反代，无需单独访问 :3001） |
| 健康检查 | http://<服务器IP>/api/health |

> 部署命令：`bash deploy/setup-demo.sh`（服务器上）或本地 `bash deploy/setup-demo.sh --remote root@<IP>`

## 2. 演示账号（8 角色 + 学员）

| 角色 | 账号 | 密码 | 看点 |
|------|------|------|------|
| 超级管理员 | `admin` | foxlearn2026 | 全平台视图、组织管理、系统配置 |
| 机构管理员 | `org_admin` | foxlearn2026 | 本机构业务全貌（培训/考试/证书） |
| 招生机构管理员 | `agency_admin` | foxlearn2026 | 招生报名、学员管理 |
| 讲师 | `lecturer01` | foxlearn2026 | 授课、出题、批阅 |
| 考务员 | `exam_officer` | foxlearn2026 | 组卷、开考、成绩发布 |
| 监考 | `proctor01` | foxlearn2026 | 考试监控、签到、违规处理 |
| 审计员 | `auditor01` | foxlearn2026 | 审计日志、留痕查询 |
| 学员 | `stu001`（张三） | foxlearn2026 | 学习、应考、查成绩、领证书 |

补充账号：`stu002`~`stu005`（学员）、`branch_admin` / `dept_admin`（两个分支机构的机构管理员，用于演示**组织间数据隔离**，密码同为 foxlearn2026）。

## 3. 预置演示数据

- 科目/章节/知识点树、题库 10 题、试卷《数智化管理师（DTM）模拟测试》
- 培训班《数智化管理师（DTM）第1期》+ 5 名学员报名
- **【Demo】数智化管理师线上统考**（PUBLISHED，`stu001`/`stu002` 可立即开考）
- **【Demo】数智化管理师线下结业笔试**（SCORE_PUBLISHED，成绩已发布）
- 已签发证书 2 份：`FX-20260803-9001`（张三 86 分）/ `FX-20260803-9002`（李四 72 分），含防伪验证码，可在证书查验页核验
- 证书设计器预置 3 份模板：学员结业证书（二维码+印章）/ 培训学时证明（表格）/ 机构定制荣誉证书（Logo+防伪码）

## 4. 演示脚本（约 10 分钟）

### 场景 1｜超管全局视图（admin 登录）
1. Dashboard 看全平台培训/考试/证书指标
2. 组织管理：展示中电标协 + 华东分中心/数字人才事业部两级组织树

### 场景 2｜组织数据隔离（切换 branch_admin / dept_admin）
1. 分别登录两个分支机构管理员，各自只能看到本机构学员与数据
2. 强调：跨机构 ID 直读会被 404 拦截（有集成测试背书）

### 场景 3｜学员在线考试全链路（stu001 登录）
1. 「我的考试」→ 进入【Demo】线上统考 → 答题 → 交卷
2. 纯客观题自动判分，当场出分；如含主观题则展示「待阅卷」状态
3. 切回 `exam_officer`：阅卷/成绩发布流程

### 场景 4｜线下考试 + 证书签发（exam_officer / admin）
1. 线下考试管理：查看【Demo】线下结业笔试的完整状态机（已发布成绩）
2. 证书管理：查看 FX-20260803-9001/9002，演示「成绩发布自动触发证书申请」
3. 打开证书查验页，输入编号/防伪码公开核验

### 场景 5｜证书设计器（admin → 证书模板）★亮点
1. 打开预置 3 份模板，画布拖拽式设计器实时预览
2. 修改变量示例数据 → 预览 → 一键生成 PDF（画布与 PDF 同源渲染，零差异）
3. 展示批量生成（ZIP 下载）

### 场景 6｜审计留痕（auditor01 登录）
1. 审计日志页查询上述所有操作的留痕记录

## 5. 运维命令

```bash
docker compose ps                       # 查看容器状态
docker compose logs -f server           # 后端日志
docker compose run --rm seed            # 重置/补齐演示数据（幂等）
docker compose down && docker compose up -d --build   # 重新构建
bash scripts/backup-db.sh               # 数据库备份
```

## 6. 已知边界

- 短信/邮件通知为框架占位（未接真实通道）
- 视频课程使用外链示例，未接腾讯云 VOD
