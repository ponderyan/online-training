# FoxLearn 教材文档解析 / OCR 依赖（Python 3.9+）
# 安装：python3 -m pip install -r requirements.txt
#
# 主引擎 RapidOCR（PaddleOCR 模型 ONNX 化，Apache 2.0，纯 CPU 可跑）：
#   - 若平台无对应轮子（如 alpine/musl），安装失败时自动降级 Tesseract(chi_sim)，链路不中断
rapidocr-onnxruntime>=1.3.0
# PDF 扫描件逐页渲染
pymupdf>=1.24
# PPTX 文本 + 内嵌图片提取
python-pptx>=1.0
# 降级引擎 Tesseract（需系统包：apt install tesseract-ocr tesseract-ocr-chi-sim / apk add tesseract-ocr tesseract-ocr-chi-sim）
pytesseract>=0.3
