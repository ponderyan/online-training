# ═══ FoxLearn Demo 环境配置（复制为 .env 后按需修改）═══
# 访问地址：http://<服务器公网IP>（绑定域名则填域名）
SITE_URL=http://YOUR_SERVER_IP

# 数据库（如无特殊需要保持默认）
DB_ROOT_PASSWORD=foxlearn_root_2026
DB_NAME=online_training
DB_USER=training_user
DB_PASSWORD=training_2024

# JWT 密钥（部署脚本会自动生成随机值覆盖）
JWT_SECRET=CHANGE_ME_RANDOM_32

# 所有演示账号统一密码（8 角色 + 学员）
DEMO_PASSWORD=foxlearn2026
